name: Test
on: push
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        run: cd core && npm ci
      - name: Run tests
        run: cd core && npm run test

